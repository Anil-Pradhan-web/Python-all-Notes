# Q5. Password Reset Functionality

## Step 1: Configure Email Backend (`settings.py`)

```python
EMAIL_BACKEND = 'django.core.mail.backends.console.EmailBackend'
```

## Step 2: Add URLs (`urls.py`)

```python
path(
    'password-reset/',
    auth_views.PasswordResetView.as_view(),
    name='password_reset'
),
```

## Output

```text
Password reset email generated
Token-based reset working
```
